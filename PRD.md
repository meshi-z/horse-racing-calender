# 重賞カレンダーサービス プロダクト要求仕様書 (PRD)

| 項目 | 内容 |
| :--- | :--- |
| **プロダクト名** | horse-racing-calendar Web アプリケーション (MVP) |
| **作成日** | 2026年9月5日 |
| **バージョン** | v1.1.0 (MVP) |
| **配信形式** | SPA / PWA (GitHub Pages ホスティング) |

---

## 1. プロジェクト概要

JRA（日本中央競馬会）の重賞レース（G1, G2, G3等）を中心に、年間・月間の開催スケジュールを視覚的かつ軽快に確認できるWebアプリケーション（リポジトリ名: `horse-racing-calendar`）。

モバイル閲覧時はタイムライン形式、PC/タブレット閲覧時は月間カレンダー形式を初期表示とし、PWAおよびオフライン閲覧に対応することで、競馬場や外出先などの電波状況が不安定な環境でもストレスなくアクセスできる体験を提供する。

---

## 2. コア要件と開発ロードマップ

### フェーズ 1 (MVP / 現在のスコープ)
- **データソース**: JRA公式提供の `.ics`（iCalendar）手動/コマンドインポート
- **提供形式**: ユーザー登録不要のオープン型 Web アプリ (SPA)
- **ホスティング**: GitHub Pages（静的配信・コスト0運用）
- **オフライン対応**: PWA (Service Worker) によるローカルキャッシュと高速表示

### フェーズ 2 (将来拡張)
- **データ拡張**: NAR（地方競馬ダートグレード）および海外主要レース（米・豪・香港・サウジ・ドバイ・欧州）の拡張
- **自動化**: GitHub Actions による `.ics` 自動取得・更新パッチ処理
- **インフラ拡張**: Firebase / Vercel への移行（動的機能やバックエンドが必要になった場合）
- **フィルタリング拡張**: 距離別（スプリント・マイル・クラシック・ディスタンス等）、馬場別フィルタ

---

## 3. カラーパレット仕様 (グレード色分け)

公式の重賞ブランド表記および動画帯色に準拠した配色を採用。

| グレード | カラーネーム | HEX コード | 該当レース例 | 備考 |
| :--- | :--- | :--- | :--- | :--- |
| **G1 / J.G1** | ブルー (Blue) | `#2563EB` | 桜花賞、日本ダービー、有馬記念など | 最高峰グレード |
| **G2 / J.G2** | レッド (Red) | `#DC2626` | チューリップ賞、金鯱賞など | 主要ステップレース |
| **G3 / J.G3** | グリーン (Green) | `#16A34A` | きさらぎ賞、ファルコンSなど | 重賞競走 |

---

## 4. UI/UX 仕様

### 4.1 初期ビュー判定と表示記憶
1. **初期ビュー分岐 (端末判定)**
   - **モバイル端末 (`< 768px`)**: 直近・今後の重賞が並ぶ **「タイムラインビュー」**
   - **PC / タブレット (`>= 768px`)**: 全体感を把握しやすい **「月間カレンダービュー」**
2. **ユーザー設定の永続化**
   - ユーザーが手動でビューを切替えた場合、選択（`timeline` or `calendar`）を `localStorage` に保持。次回訪問時はデバイス幅に関わらず保持された設定を優先。

### 4.2 タイムゾーン自動変換（グローバル対応）
- JSON内の発走時刻（UTC）を、クライアント側のJavaScriptでアクセス元のローカルタイムゾーン（ブラウザ設定）へ動的に変換して画面表示する。

### 4.3 フィルタリング機能
- **重賞グレード絞り込み**: All / G1 / G2 / G3
- **競馬場絞り込み**: 東京、中山、阪神、京都、中京、小倉、新潟、福島、札幌、函館
- **(将来拡張用フィールド)**: 国・団体、距離区分

---

## 5. データ構造 & パイプライン

### 5.1 拡張 JSON スキーマ (`public/data/races.json`)
発走時刻 `start_time` はすべて **UTC ISO-8601 形式 (`Z`)** で保持する。

```json
{
  "$schema": "[http://json-schema.org/draft-07/schema#](http://json-schema.org/draft-07/schema#)",
  "version": "1.1.0",
  "updated_at": "2026-09-05T18:00:00Z",
  "races": [
    {
      "id": "jra-2026-ouka-sho",
      "title": "第86回 桜花賞",
      "grade": "G1",
      "organization": "JRA",
      "country_code": "JP",
      "venue": "阪神競馬場",
      "surface": "Turf",
      "distance_m": 1600,
      "start_time": "2026-04-12T06:40:00Z",
      "time_zone": "Asia/Tokyo",
      "raw_summary": "第86回 桜花賞（GⅠ）"
    }
  ]
}
```

### 5.2 .ics パーサー (データ変換ロジック) 要件
- JRA公式 `.ics` の `SUMMARY` （例: `第86回 桜花賞（GⅠ）`）から、正規表現を用いて **「重賞名」「回数」「グレード (G1/G2/G3)」** をパーシング。
- `LOCATION` または `SUMMARY` から競馬場名を抽出・標準化。
- JST等で記載されている日時を UTC 形式に変換 して start_time フィールドへ出力する。

---

## 6. 技術スタック選定基準

- **フロントエンド**: React + TypeScript (Vite) または Next.js (SSG)
- **スタイリング**: Tailwind CSS
- **PWA / Cache**: `vite-plugin-pwa` (Workbox)
- **インフラ**: GitHub Pages (GitHub Actions でビルド・デプロイ)
- **セキュリティ・品質方針**: 活発なオープンソースコミュニティを持つライブラリで構成し、GitHub Dependabot を有効化して脆弱性を自動でアップデート・検知。

---

## 7. Antigravity 連携手順

1. 本仕様書を `PRD.md` というファイル名でリポジトリ直下に保存。
2. Antigravityのプロンプトで `PRD.md` を読み込ませ、「`.ics` から `public/data/races.json` への変換スクリプトから実装を開始して」と指示。
